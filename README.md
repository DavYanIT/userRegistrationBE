# User Registration
#### Back-end

To run the server:


```
> npm install
> mongod --dbpath <path_of_data_folder>
```
In new terminal window:

```
> mongo
> use local
```
In new terminal window:

```
> npm start
```
